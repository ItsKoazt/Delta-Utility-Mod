name: Publish to Modrinth

on:
  release:
    types: [published]
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 25

      - name: Build
        # Derive the mod version from the tag (v1.2.0 -> 1.2.0) so the jar's
        # internal version can never drift from the released version.
        run: ./gradlew build -Pmod_version=${GITHUB_REF_NAME#v}

      - name: Publish to Modrinth and GitHub
        uses: Kir-Antipov/mc-publish@v3.3
        with:
          modrinth-id: kzlaVC0v
          modrinth-token: ${{ secrets.MODRINTH_TOKEN }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          files: build/libs/Delta.jar
          name: Delta Utility Mod ${{ github.ref_name }}
          version: ${{ github.ref_name }}
          version-type: release
          loaders: fabric
          game-versions: "26.2"
          changelog-file: CHANGELOG.md
          dependencies: |
            fabric-api(required)
            modmenu(optional)
